import { colors } from "@/constants/colors";

/** Circumference for ring with r=24 (matches60×60 viewBox). */
export const BREATHING_RING_CIRC = 2 * Math.PI * 24;

export const BREATHING_COL_WARN = colors.warmAccent;

export const BREATHING_EXHALE_LEGEND = colors.blueExhale;

export const BREATHING_DOT_ACTIVE = colors.blueMid;

export const BREATHING_DOT_IDLE = colors.borderInput;
